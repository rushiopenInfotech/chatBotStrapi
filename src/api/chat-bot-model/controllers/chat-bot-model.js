'use strict';

/**
 * chat-bot-model controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::chat-bot-model.chat-bot-model');
